{
  "name": "megha-social-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "check": "node --check public/app.js && node --check scripts/serve.mjs && node --check scripts/build.mjs"
  },
  "engines": { "node": ">=20" }
}
